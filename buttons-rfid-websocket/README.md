Python eksempel for å lytte på RFID via ``SimpleMFRC522``, og 3 knapper på GPIO 8, 10 og 12.

Sender events over en websocket-server når det skjer noe.

Krever følgende pakker:

- `asyncio`
- `websockets`
- `RPi.GPIO`
- `SimpleMFRC522`
